# Zwerg Configuration File

watches:
  - path: "."
    recursive: false
    debounce: 500
    filters:
      ignore_access: true
    patterns:
      - "*.re"
    actions:
        - command: "bundle exec rake pdf || notify-send 'PDF Build Failed' 'The PDF generation command failed with non-zero exit status'"
