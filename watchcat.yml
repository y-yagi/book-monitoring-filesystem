# Zwerg Configuration File

watches:
  - path: "."
    recursive: false
    debounce: 1000
    filters:
      ignore_access: true
    patterns:
      - "*.re"
    actions:
        - command: "bundle exec rake pdf"
